# Supabase project credentials. Find these in:
#   Supabase dashboard → Project settings → API
#
# These two values are safe to ship to the browser. Row-Level-Security
# policies in `database.sql` ensure the anon key can only read.
NEXT_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOi...
